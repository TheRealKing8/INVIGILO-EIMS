-r base.txt

# --- Testing -----------------------------------------------------------------
pytest==8.3.4
pytest-django==4.9.0
pytest-cov==6.0.0
freezegun==1.5.1
factory-boy==3.3.1
responses==0.25.3

# --- Linting / typing --------------------------------------------------------
ruff==0.7.4
black==24.10.0
mypy==1.13.0
django-stubs[compatible-mypy]==5.2.7
djangorestframework-stubs==3.15.2
types-requests==2.32.0.20240914

# --- AI assistant (Phase 19) ------------------------------------------------
# The OpenRouter client is a thin async wrapper around httpx. Pinned to
# the same major as the test mocks use.
httpx==0.28.1
